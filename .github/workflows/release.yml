name: Release

# Builds and signs the release APK, verifies the signer, and attaches it to
# the GitHub Release for the pushed tag. Triggered ONLY by a tag push
# (v*); the tag initiator explicitly authorizes this mechanical release build.
#
# Required repo secrets (set these yourself in GitHub Settings -> Secrets
# and variables -> Actions; nothing here or in any agent session ever sees
# the actual values):
#   RELEASE_KEYSTORE_BASE64   base64 of hermes-release.jks (`base64 -w0 hermes-release.jks`)
#   RELEASE_KEYSTORE_PASSWORD
#   RELEASE_KEY_ALIAS
#   RELEASE_KEY_PASSWORD

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write   # needed to create the release / upload the asset

jobs:
  build-sign-release:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      # A tag push is an explicit instruction to cut a release. If the signing
      # secrets are missing there is nothing this job can produce, so it fails
      # loudly here rather than skipping every remaining step and reporting
      # success — a green Release run must always mean "a signed APK shipped".
      # To release by hand instead, delete the tag rather than pushing it.
      - name: Require signing secrets
        run: |
          missing=""
          [ -n "${{ secrets.RELEASE_KEYSTORE_BASE64 }}" ]   || missing="$missing RELEASE_KEYSTORE_BASE64"
          [ -n "${{ secrets.RELEASE_KEYSTORE_PASSWORD }}" ] || missing="$missing RELEASE_KEYSTORE_PASSWORD"
          [ -n "${{ secrets.RELEASE_KEY_ALIAS }}" ]         || missing="$missing RELEASE_KEY_ALIAS"
          [ -n "${{ secrets.RELEASE_KEY_PASSWORD }}" ]      || missing="$missing RELEASE_KEY_PASSWORD"
          if [ -n "$missing" ]; then
            echo "::error::Cannot build a signed release. Missing repo secrets:$missing"
            echo "Set them under Settings -> Secrets and variables -> Actions, then re-run this job."
            exit 1
          fi

      - uses: actions/checkout@v4
        with:
          submodules: recursive

      # The :core:* Gradle projects live in the separate agent-core repo and are
      # mapped in by settings.gradle.kts. agent-core.ref pins the engine commit,
      # so the APK attached to a tag is reproducible from two recorded SHAs.
      - name: Resolve pinned agent-core ref
        id: engine
        run: |
          REF=$(grep -vE '^\s*(#|$)' agent-core.ref | head -1)
          if [ -z "$REF" ]; then
            echo "::error::agent-core.ref contains no ref"
            exit 1
          fi
          echo "ref=$REF" >> "$GITHUB_OUTPUT"
          echo "Building against agent-core @ $REF"

      - name: Check out the agent-core engine
        uses: actions/checkout@v4
        with:
          repository: l3ad3r1/agent-core
          ref: ${{ steps.engine.outputs.ref }}
          path: agent-core

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - uses: gradle/actions/setup-gradle@v4

      - name: Write signing config (from secrets, runner-local only)
        run: |
          echo "${{ secrets.RELEASE_KEYSTORE_BASE64 }}" | base64 -d > "$RUNNER_TEMP/release.jks"
          cat > hermes.local.properties <<EOF
          hermes.signing.storeFile=$RUNNER_TEMP/release.jks
          hermes.signing.storePassword=${{ secrets.RELEASE_KEYSTORE_PASSWORD }}
          hermes.signing.keyAlias=${{ secrets.RELEASE_KEY_ALIAS }}
          hermes.signing.keyPassword=${{ secrets.RELEASE_KEY_PASSWORD }}
          EOF

      - name: Build signed release APK
        run: ./gradlew --no-daemon :app:assembleRelease

      # L-012: verify the signer before anything ships. Hard-fails the job
      # rather than publishing an APK signed with the wrong (or no) key.
      - name: Verify signer
        run: |
          APK=$(find app/build/outputs/apk/release -name '*.apk' | head -1)
          echo "Checking: $APK"
          FINGERPRINT=$("$ANDROID_HOME/build-tools"/*/apksigner verify --print-certs "$APK" \
            | grep 'signer #1 certificate SHA-256' | awk '{print $NF}')
          echo "Signer SHA-256: $FINGERPRINT"
          if [ "$FINGERPRINT" != "99255c31ffba1932e4ab2abc12d99b82bf780874b8c686076497157996cf6d6f" ]; then
            echo "::error::Signer mismatch - expected 99255c31..., got $FINGERPRINT"
            exit 1
          fi
          echo "APK_PATH=$APK" >> "$GITHUB_ENV"

      - name: Attach APK to the GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.ref_name }}"
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "$APK_PATH" --clobber
          else
            gh release create "$TAG" "$APK_PATH" --title "Jeeves $TAG" --generate-notes
          fi
